// DashboardNavbar — re-exports the global Navbar with dashboard-specific search + menu props
// This sits in components/ as a thin wrapper used only by DashboardPage
export { default } from './Navbar';
